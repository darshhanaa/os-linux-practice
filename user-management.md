# User Management

## Commands

- useradd name → create user
- passwd name → set password
- userdel name → delete user
- su username → switch user
- exit → logout

---

## Notes

Some commands require admin (sudo) privileges in real systems.
